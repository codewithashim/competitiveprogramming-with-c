#include <stdio.h>
#define MAX_LEN 100

int main()
{

    const float Velue_of_PI = 3.1416;
    printf("The value of PI is %f\n", Velue_of_PI);
    printf("max lant of this numebr is %d\n", MAX_LEN);

    return 0;
}

//if we use use const variable in the function, we can't change the value of the variable.
